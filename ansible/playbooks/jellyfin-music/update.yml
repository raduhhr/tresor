---
# Update Jellyfin Media: backup current state â†’ deploy new version â†’ verify health
# Prerequisite: bump the version in group_vars/prod/versions.yml first
- name: "Update Jellyfin Media â€” backup"
  import_playbook: backup.yml

- name: "Update Jellyfin Media â€” deploy"
  import_playbook: deploy.yml

- name: "Update Jellyfin Media â€” status"
  import_playbook: status.yml
