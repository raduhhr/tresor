---
- name: Stop Jellyfin Music
  hosts: tresor
  become: true
  gather_facts: false

  vars:
    container_name: jellyfin-music

  tasks:
    - name: Stop Jellyfin Music container
      community.docker.docker_container:
        name: "{{ container_name }}"
        state: stopped
      register: _stop

    - name: Show status after stop
      ansible.builtin.debug:
        msg: "Jellyfin Music stopped successfully"
      when: _stop.changed
