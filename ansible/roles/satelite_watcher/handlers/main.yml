---
- name: systemd daemon reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart satelite watcher scheduler timer
  ansible.builtin.systemd:
    name: "{{ satelite_watcher_scheduler_service_name }}.timer"
    state: restarted
    enabled: true
