---
- name: Restart Jellyfin Media
  hosts: tresor
  become: true
  gather_facts: false

  vars:
    container_name: jellyfin-music

  tasks:
    - name: Restart Jellyfin Media container
      community.docker.docker_container:
        name: "{{ container_name }}"
        restart: true
      register: _restart

    - name: Show status after restart
      ansible.builtin.debug:
        msg: "Jellyfin Media restarted successfully"
      when: _restart.changed
